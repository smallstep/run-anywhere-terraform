# run-anywhere-terraform

Terraform for the infrastructure a Smallstep Run Anywhere installation needs.

- `aws/` — two roots (`platform`, `workloads`) and the modules they are built from. See `aws/README.md`.
- `gcp/`, `azure/` — one module per cloud.
